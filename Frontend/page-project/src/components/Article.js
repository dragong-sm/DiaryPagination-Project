// 콘텐츠 제목부분

// 제목 + 날짜표시
// 작성일시(writtenDate), 수정일시(modifiedDate) 둘 다 표시

import React from "react";
import styled from "styled-components";

const Article = ({ titles }) => {
  console.log(titles);
  return (
    <div>
      {/* <h1>제목 : {title} </h1>
      <p>일시 : {date}</p> */}
    </div>
  );
};

export default Article;
