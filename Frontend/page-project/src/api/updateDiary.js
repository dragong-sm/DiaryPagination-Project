// 다이어리 수정하기 (edit)

import axios from "axios";
import { useEffect, useState } from "react";

export const updateDiary = async ({}) => {
  // http://localhost:8080/diary/${postNo}
  // const () = axios.put();
};
